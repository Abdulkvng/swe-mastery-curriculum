[package]
name = "stage3-tls"
version = "0.1.0"
edition.workspace = true

[dependencies]
# rustls = pure-Rust TLS implementation, no OpenSSL dependency.
# Used in production by Cloudflare, AWS, Discord, etc.
rustls = "0.23"
# Helper for loading PEM-encoded certs and keys.
rustls-pemfile = "2"
